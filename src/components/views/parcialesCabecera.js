import React from 'react';

const ParcialesCabecera = (props) => {
  //tenque que hallar los realiza de un determinado alumno en un determinado trimestre
  //. Para cada realiza hallar el parcial (nombre y peso) mediante su id.

  return (
    <th>
      El nombre del parcial
    </th>
  )
}

export default ParcialesCabecera;